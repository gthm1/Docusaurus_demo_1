# Copy this file to .env.local and fill in the values from app.tina.io
# after connecting this repo (see DEMO_WALKTHROUGH.md, "TinaCMS setup").
# .env.local is gitignored — never commit real credentials.

NEXT_PUBLIC_TINA_CLIENT_ID=
TINA_TOKEN=
